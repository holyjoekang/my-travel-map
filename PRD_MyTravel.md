# 나의 여행 지도 — 30년 이동 기록 아카이브 설계

> 실명은 이 문서에도 적지 않는다. 원본은 `data/private.json`(저장소 밖)에 있다.
>
> 참조: 같은 방식으로 만든 『나의 서재』 PRD(`PRD.md`)와 프로필
> (`09_AI project/50플러스 창업 및 인생준비/research/01_profile.md`).
> 서재가 "읽은 책"을 모았다면, 이 앱은 **"내가 서 있었던 자리"**를 모은다.

## 1. 컨셉

1996년 입사 이후 30년간의 **출장·주재·여행 기록을 한 장의 지도와 한 줄의 연표**로 모으는 개인 아카이브.

- 기록의 성격이 서재와 다르다. 책은 "한 권"이 단위지만, 여행은 **"언제 · 어디를 · 누구와 · 왜"** 네 축이 모두 있어야 하나의 기록이 된다.
- 그래서 단순 여행 블로그가 아니라, **도시(Place)를 1급 개체로 두는 데이터베이스**로 설계한다. "중국 100여 개 도시를 다녔다"는 말이 앱 안에서 세어지는 숫자가 되어야 한다.
- 시간축(연표)과 공간축(지도)을 같은 데이터로 동시에 본다.
- 소스는 **네이버 블로그 + Google Docs/Sheets**. 여기에 **Google Maps로 좌표·지도**를, **내 사진첩에서 EXIF로 사진**을 붙인다.
- 서재와 마찬가지로 **자동 등록 금지**. 수집은 후보로 쌓고 사용자가 승인한다.

## 2. 핵심 기능

| # | 기능 | 설명 |
|---|------|------|
| 1 | 블로그 자동 수집 | 네이버 블로그 여행 관련 카테고리 글 → 여정 후보로 변환 (§3-1 검증 완료) |
| 2 | Google Drive 수집 | 출장 이력 스프레드시트·여행 일지 Docs → 여정으로 변환 (§3-2 검증 완료) |
| 3 | 도시 정규화 | 하문/샤먼/Xiamen을 한 도시로 합치고 좌표를 확정 (§5, 이 프로젝트 최대 난제) |
| 4 | 연표 뷰 | 1996~2026을 7개 시대 레인으로. 경력과 이동이 같은 화면에 겹친다 |
| 5 | 지도 뷰 | 세계지도 + 중국 확대. 점 크기 = 방문 횟수, 색 = 목적 |
| 6 | 지역 뷰 | 대륙 > 국가 > 도시 트리. 도시를 누르면 그 도시의 모든 방문 이력 |
| 7 | 사진 추가 | 내 사진첩에서 끌어놓기 → EXIF 촬영일시·GPS로 여정에 자동 배속 |
| 8 | 여정 상세 | 일정·동행·구간 지도·사진·블로그 원문 링크 |
| 9 | 기억 입력 | 디지털 기록이 없는 1996~2004 구간을 기억으로 채우는 전용 입력 (§3-5) |
| 10 | 통계 | 총 도시/국가 수, 첫 방문, 최다 방문, 연도별 이동 |
| 11 | 공개 모드 | 실명·거래선을 지운 공개용 빌드를 따로 만든다 (§11) |

## 3. 소스 검증 — 실측 결과

설계 전에 블로그·드라이브·캘린더를 실제로 호출해 확인했다. **결과가 로드맵을 바꿨다.**

### 3-1. 네이버 블로그 (blog.naver.com/joekang)

엔드포인트와 주의사항은 서재 PRD §3과 동일하다(`Referer` 헤더 필수, 목록 JSON은 정규식 파싱, 제목은 URL 인코딩). 서재의 `scripts/fetch_blog_posts.py`를 그대로 재사용할 수 있다.

전체 19개 카테고리 중 여행이 들어있는 곳:

| categoryNo | 카테고리 | 글 수 | 연도 분포 | 여행 관련성 |
|---|---|---|---|---|
| 21 | 달리기,여행,맛집 | 150 | **2020~2026만** | 높음. 단 러닝·영화·맛집이 섞임 |
| 17 | 지구촌 알쓸신잡 | 225 | **2006~2026** | 높음. 단 최근은 중국 시사 칼럼 |
| 1 | 일상 생각,에세이 | 312 | 2003~2026 | 중간. 가족여행 일부 |
| 19 | 신앙생활 | 935 | 2019~2026 | 낮음. 단 성지순례가 여기 |
| 25 | 스타트업 + 글로벌 | 31 | 2021~2026 | 낮음 |
| 32 | 힐링 모먼트 | 1,397 | — | **미확인. 아래 참조** |

**발견 1 — 여행 카테고리는 2020년에 생겼다.** cat 21은 150편 전부가 2020년 이후다. 즉 "여행 카테고리를 긁으면 30년이 나온다"는 기대는 틀렸다.

**발견 2 — 옛 여행기는 cat 17에 있고, 성격이 중간에 바뀌었다.** cat 17의 2006년 15편은 지역전문가 시절 현장 기록 그 자체다.

```
2006.7.17  2005년7월 첫 여름 여행 내몽고 후룬베이얼
2006.6.5   동방 유태인들의 고향, 중국 온주(溫州) 를 가다
2006.6.5   중국에서 가장 친절했던 둔황
2006.6.5   북경 최고 골동품 시장 판쟈위엔에서 쇼핑하기
```

반면 같은 카테고리의 2026년 글은 「중국 '도시 뇌'의 역습」 같은 시사 칼럼이다. **카테고리 번호로는 여행 글을 못 거른다. 서재에서 책 판정에 LLM을 쓴 것과 똑같이, 여기서도 LLM 판정이 필요하다.**

**발견 3 — '힐링 모먼트' 1,397편이 API로 안 잡힌다.** 카테고리 목록에는 1,397편으로 잡히는데 `PostTitleListAsync`가 0건을 돌려준다. 네이버 '모먼트'(동영상·사진 포맷)로 추정된다. 전체 2,516편 중 가장 큰 덩어리이고 사진이 많을 가능성이 높아 **별도 확인이 필요하다**(§13).

수집 대상 합계: **cat 21+17+1 = 687편**을 LLM에 태워 여행 글을 거른다. cat 19는 성지순례 키워드로 선별 투입.

### 3-2. Google Drive — 여기가 진짜 광맥이다

`여행, 중국, 국내 여행` 폴더(파일 35개)와 그 밖의 문서를 확인했다.

**(1) 이미 표로 정리된 출장 이력이 있다.** 스프레드시트 `출장 16년~19년, 해외`는 **28건이 날짜·지역·목적·직책까지 구조화**되어 있다. LLM 없이 바로 파싱된다.

> **시트가 한 줄로만 적어 둔 것은 기억으로 채운다 (2026-09).** `17년7월8일~16일 | 미국 필드트립`
> 한 줄이 도시 없이 남아 있었고, 별칭 사전이 그것을 실리콘밸리로 **추정**해 두고 있었다.
> 본인이 하루 단위 일정을 적어 주어 **뉴욕 엿새 + 뉴헤이븐 예일대 이틀**로 확정하고,
> 추정 별칭은 걷어냈다. 다만 시트(7.8~7.16)와 기억(7.11~7.18)이 사흘 어긋나 `note`에 함께 남겼다.
> **시트의 한 줄이 틀린 추정으로 굳는 것을 막으려면, 추정 별칭에는 반드시 근거를 적어 두어야 한다.**

```
16년8월2일~8월4일 | 대만, 하문      | ○○ △사장 사업미팅          | 중국파트장
19년2월11일~16일  | 방콕, 뉴델리    | ○○○과 출장, 첫 인도 출장   | 해외사업그룹장
19년11월4일~8일   | 대만, 홍콩, 방콕 | ○○○상무와 마지막 출장      | 해외사업그룹장
```

→ **파일 하나로 28개 여정, 10개국 이상이 즉시 채워진다.** 앱이 첫날부터 비어 보이지 않는다.

**(2) 최근 여행은 일지가 통째로 있다.** 윈난(2026.4), 충칭(2026.6), 대련(2026.7), 대만(2024.12), 오사카(2024.5), 북경 자유여행(2024.11), 남해·유명산(2025), 상해 출장(2025.5), 중국 물류 출장(2025.11) 등. 일부는 **20~30MB짜리 Docs — 본문에 사진이 박혀 있다는 뜻**이다. 사진을 따로 찾을 필요 없이 문서에서 추출할 수 있다.

**(3) 라이프로그 스프레드시트.** `국내외여행, 중국, 싱가폴, 대만, 제주도 등`은 이름과 달리 여행 마스터가 아니라 **캘린더 덤프**다(Start/End/Title/Location/Category, 2025년치). Category는 업무·가족/지인·식사/모임·운동·금융·건강·반려동물 7종. 여행 전용은 아니지만 **날짜 정확도가 가장 높은 소스**라 여정의 시작·종료일 확정에 쓴다.

**(4) 중국 원고가 맥락을 준다.** 『중국, 나를 키운 거대한 교실』, 『우리가 모르는 중국』 원고가 있다. 여정에 붙일 회고 텍스트의 출처로 쓸 수 있다(여정 → 이야기 연결).

### 3-3. Google Calendar

캘린더가 두 개다: `holyjoekang@gmail.com`(기본, 여행 이벤트 검색 결과 0건)과 **`shinjoe.kang@gmail.com`(강신조)**. §3-2(3)의 라이프로그가 후자에서 나온 것으로 보인다. **어느 계정이 언제부터 기록을 갖고 있는지 확인이 필요하다**(§13).

### 3-4. 사진

Google Photos API는 2025년 정책 변경으로 앱이 라이브러리 전체를 읽을 수 없다(사용자가 고른 사진만 넘겨주는 Picker 방식). 따라서 **연동에 기대지 않고, 로컬 사진첩 폴더를 끌어놓는 방식을 1순위**로 한다. 서재의 `scripts/prepare_photos.py`(긴 변 1600px 축소 + EXIF 회전 보정)를 그대로 쓰고, 거기에 **EXIF 촬영일시·GPS 추출을 더한다**. 배속 규칙은 §7.

### 3-5. 공백 구간 — 진짜 빈 곳은 1996~2004다

| 시기 | 디지털 기록 |
|---|---|
| 1996~2002 첫 출장들 (싱가포르·말레이시아, 런던, 뉴욕/뉴저지) | **없음.** 블로그는 2003년 시작 |
| 2003~2004 | 블로그 cat 1에 5편뿐 |
| 2005~2006 중국지역전문가 | **해결됨.** Google 지도 저장 목록 128개 수집 완료(§3-6) |

**이것이 서재의 "초·중·고·대 시절은 수동 입력"과 정확히 같은 구조다.** 서재가 `data/memory_books.json`에 기억으로 복원한 66권을 넣었듯, 여기도 `data/memory_trips.json`이 필요하다.

다만 **가장 큰 걱정이었던 지역전문가 시절은 §3-6의 저장 목록 128개로 해결됐다.** 진짜 공백은 E1~E2(1996~2004)로 좁혀졌고, 이 구간은 여정 수 자체가 적어 기억으로 복원할 만하다.

> 참고: 본인은 지역전문가 기간을 "2005.4~2006.3"으로, 공식 이력서는 "2004.12~2006.4"로 적고 있다. 앱에는 이력서 기준을 넣고 기억은 메모로 남긴다.

### 3-6. Google 지도 저장 목록 — 수집 완료 ✅ (128개)

지역전문가 시절 방문지가 Google 지도 저장 목록 **「2005년 지역전문가」**에 들어 있다. 공유 링크로 **128개 전체를 수집해 `data/gmaps_saved_2005.json`에 넣었다.**

**수집 방법의 함정.** 목록이 무한 스크롤이라 처음엔 20개만 잡혔다. `scrollTop`을 직접 옮기는 방식으로는 더 안 불러온다 — 구글이 **실제 휠 이벤트에만 반응**한다. 합성 `WheelEvent`를 함께 보내니 128개가 모두 로드됐다. 나중에 스크립트로 자동화할 때도 이 점이 필요하다.

**얻은 것과 못 얻은 것이 갈린다.**

| | 결과 |
|---|---|
| 얻음 | 장소 이름(한국어·중국어·영어 혼재), 국가, 성·지급시 등 **행정구역**, 장소 종류(공항/호텔/공원 등) |
| **못 얻음** | **Place ID, 좌표, 장소 URL** — 저장 목록 화면에 노출되지 않는다 |

따라서 "Place ID가 이미 붙어 있으니 정규화가 공짜"라는 앞선 기대는 **틀렸다.** 좌표는 §8의 빌드 타임 Geocoding으로 따로 확정해야 한다. 다만 각 항목에 **행정구역이 함께 있어서**(예: `둔황 시 / 중국 / Gansu, Jiuquan`) 이름만 있을 때보다 지오코딩 정확도가 훨씬 높다. Place ID가 꼭 필요하면 Takeout CSV(URL 열에 CID 포함)를 받아야 한다.

**구성 — 세 단계가 섞여 있다.**

| 단계 | 수 | 예 |
|---|---|---|
| 도시(city) | 56 | 베이징, 상하이, 하얼빈, 우루무치, 둔황, 샤먼 |
| 지구·자치주(prefecture) | 5 | 카스 지구, 투루판 지구, 시솽반나 다이족 자치주, 옌볜 조선족 자치주, 아러타이 지구 |
| 구·현(district) | 5 | 툰시 구, 양숴 현, 핑야오 현, 타이산 구, 침사추이 |
| 특별행정구(territory) | 2 | 홍콩, 마카오 |
| **장소(poi)** | **60** | 자금성, 만리장성, 병마용, 룽먼 석굴, 백두산 천지, 카나스 호수 — 그리고 **호텔·식당·공원·아파트** |

**도시급으로 세면 63곳, 중국 27개 성·직할시에 걸쳐 있다.** 산둥 7, 허베이 6, 신장·내몽고·광둥·윈난 각 4 순이다. "100여 개 도시"라는 기억보다는 적지만, 저장 목록에 안 들어간 도시가 더 있을 수 있고 POI 60개를 도시로 환산하면 체감과 맞는다.

**설계에 직접 영향을 주는 발견 셋.**

**(1) 이 목록은 §5 별칭 사전의 정답지다.** `샤먼 시`(목록) = `하문`(Drive 출장표), `원저우 시`(목록) = `온주`(블로그 2006년 글), `후룬베이얼 시`(목록) = `내몽고 후룬베이얼`(블로그). **세 소스가 같은 도시를 세 가지로 부르는 실물 증거**가 확보됐다. 별칭 사전의 첫 항목들은 여기서 그대로 나온다.

**(2) POI 60개가 도시에 매달린다.** 베이징에만 21개(자금성·천단·경산·베이징대·칭화대·차오양공원·호텔·식당), 톈진 4개(리츠칼튼·거우부리·하이허 유람선·우다다오)다. §6에서 "도시 페이지가 이 앱의 하이라이트"라고 한 설계가 **데이터로 뒷받침된다.** 베이징 페이지는 첫날부터 21개 장소로 채워진다.

**(3) 생활 흔적이 섞여 있다 — 지우지 말 것.** `Huaqingjiayuan 12 Building`(华清嘉园, 칭화대 인근 아파트), `阳光上东`, 동네 헬스장, 단골 만두집. 관광지가 아니라 **그때 거기서 살았다는 기록**이다. 여행 아카이브에서 가장 사적이고 값진 부분이라 `kind: 주거`로 따로 표시해 두었다.

**한계 — 목록에 날짜가 없다.** "어디"만 있고 "언제"가 없다. 그래서 이 장소들은 **Place는 확정, Trip은 미확정**으로 들어온다.

- E3(2004.12~2006.4)을 덮는 **"지역전문가" 포괄 Trip 하나**에 전부 매단다.
- 블로그 cat 17에 글이 있는 곳(후룬베이얼 2005.7, 원저우, 둔황)은 글 날짜로 **개별 Trip으로 승격**한다.
- 나머지는 날짜 없이 둔다. 지도에는 찍히고 연표에서는 E3 레인에 묶음으로 표시된다.

> **2026-09 보탬 — 기억으로 달을 붙였다.** 본인이 장소 46곳에 추정 시기와 확실도(A~D)를 매겨
> 주어, 그중 날짜가 잡히는 것을 **월 단위 여정 14개 + 베이징 생활권 1개**로 승격했다.
> 확실도는 여정의 `confidence`(A=high, B=medium, C·D=low)와 `note`로 옮겼다.
> 장소는 **도시와 섞지 않고 여정의 `places` 에 따로 매단다** — 도시 수와 방문 집계가
> 장소 때문에 부풀지 않게 하려는 것이고, 화면에서도 '도시'와 '장소'가 따로 선다.
> 목록의 상위 도시를 바로잡거나(연우루 → 저장 자싱) 목록에 없던 곳을 보탤 때(화청궁)는
> 캡처 원본을 고치지 않고 `data/place_fixes.json` 에 적어 빌드에서 덮어쓴다.
- **확인 필요 12건**에 `needsReview` 플래그를 달았다(상위 도시가 불분명한 장소, 목록에 도시가 없는 곳 — 황궈수 폭포/안순, 주자이거우/아바주 등).
- 목록 이름이 「2005년 지역전문가」로 시기가 명시돼 있어, 다른 시기 장소가 섞였을 걱정은 낮다. 다만 마카오·홍콩·침사추이는 다른 시기 방문일 수 있어 승인 화면에서 확인한다.

## 4. 시대 구분 (Era)

프로필의 경력과 1:1로 맞춘다. 연표의 가로 레인이 되고, 여정의 기본 분류축이 된다.

| # | 시대 | 기간 | 직책 | 주 이동 | 소스 |
|---|---|---|---|---|---|
| E1 | 첫 출장 | 1996.2~2000.2 | 해외시스템개발 대리 | **싱가포르·말레이시아(첫 출장)**, 런던, 뉴욕/뉴저지 | 기억 |
| E2 | 전략기획 | 2000.8~2004.11 | 전략기획팀 과장 | 국내 중심 | 기억, 블로그 소량 |
| E3 | **중국지역전문가** | 2004.12~2006.4 | 삼성그룹 파견(베이징) | **중국 63개 도시 + 장소 60곳** | **저장 목록 128개 ✅** + cat 17 (15편) |
| E4 | 해외사업·컨설팅 | 2006.5~2009.9 | 차장 | 전자정부 수출 대상국 | 블로그 |
| E5 | **베이징 주재** | 2009.10~2015.12 | 중국법인 총경리 | 중국 전역 + 가족여행 | 블로그 (2014~15년 55편) |
| E6 | 홈IoT 해외사업 | 2016.1~2022.8 | 해외사업그룹장 | 중화권·동남아·인도·미국·호주 | **Drive 28건** + 블로그 |
| E7 | 물류 글로벌 | 2022.8~현재 | 중국 Regional Manager | 중국 중심 | Drive + 블로그 cat 21 |

가족·부부·친구 여행은 이 축과 **독립적으로** 목적(purpose) 축에 들어간다. E5 시기의 여정 하나가 "주재 + 가족여행"일 수 있기 때문이다.

## 5. 도시 정규화 — 이 프로젝트의 최대 난제

서재에서 책 id가 밀려 수정 기록이 어긋났던 것처럼(서재 PRD 로드맵 3항), 여기서는 **같은 도시가 여러 이름으로 들어와 숫자가 틀어진다.**

실제 소스에서 확인된 혼용:

```
하문 (Drive 출장표) = 샤먼 시 (지도 저장 목록) = Xiamen = 厦门
온주 (블로그 2006) = 원저우 시 (지도 저장 목록) = 溫州
북경 (2006년 글)    = 베이징 (최근 글)
상해/상하이/上海,  청도/칭다오,  심천/선전,  총칭/충칭/重庆
```

그대로 세면 "100여 개 도시"가 150개로 부풀거나 60개로 쪼그라든다.

**설계**

- `data/place_aliases.json` — 한국식 한자 독음 ↔ 중국어 병음 표기 ↔ 한자 ↔ 영문의 4중 별칭 사전을 **사람이 관리하는 파일**로 둔다. 자동 추론에 맡기지 않는다.
- 도시 id는 **Google Place ID**를 정본으로 삼는다. 이름이 어떻든 Place ID가 같으면 한 도시다.
- **§3-6의 저장 목록 63개 도시를 먼저 넣어 기준점을 만든다.** 이름에 행정구역이 붙어 있어 지오코딩이 정확하다. 이후 블로그·출장표에서 "하문"이나 "온주"가 나와도 **새로 만들지 않고 기존 도시에 붙인다.** 별칭 사전은 그 작업의 기록이 된다 — 빈 사전을 미리 채우는 게 아니라 실제로 부딪힌 표기만 쌓인다.
- 저장 목록이 이미 정답지 세 쌍을 줬다: `샤먼 시`=`하문`, `원저우 시`=`온주`, `후룬베이얼 시`=`내몽고 후룬베이얼`.
- **나라 컨텍스트 필수.** "광주"는 한국 광주이자 중국 광저우다. "대련/다롄", "경주"도 마찬가지. 여정의 국가가 정해지기 전에는 도시를 확정하지 않는다.
- 서재의 `book_ids.json` 대장과 같은 이유로 **`data/place_ids.json` 대장**을 둔다. 사용자가 수정한 내용이 id에 붙으므로, 도시를 합치거나 이름을 고칠 때 대장의 키도 함께 옮긴다. 빠진 번호는 재사용하지 않는다.
- 한 여정이 여러 도시를 지난다(`대만, 하문`, `싱가포르, 하노이, 호치민`). **쉼표로 나열된 지역은 Leg으로 쪼갠다.** 날짜를 도시별로 못 나누는 경우가 많으므로 Leg의 날짜는 nullable로 두고 여정 날짜를 상속한다.

## 6. 데이터 모델

```
Trip (여정) — 출발부터 귀국까지 하나
  id, title, startDate, endDate, era(E1~E7)
  purpose: business | expat | family | couple | friends | pilgrimage | solo
  scope:   domestic | overseas
  companionIds[]  → Person (실명을 여기 직접 쓰지 않는다. §11)
  role            (당시 내 직책 — Drive 출장표에 이미 있다)
  summary, summaryPublic   (공개용 요약을 따로 쓴다. §11)
  coverPhotoId
  sources[]     (blog | gdoc | gsheet | calendar | gmaps_saved | memory | photo)
  confidence    (high | medium | low — 기억으로 복원한 것은 low)

Person (사람) — 실명과 공개 표기를 분리하는 유일한 지점
  id
  realName      (개인 빌드에만 들어간다. 공개 빌드에는 포함되지 않는다)
  publicLabel   (공개 표기. 사용자가 직접 정한다. 예: "Mr. K", "Ms. J")
  relation: family | colleague | client | friend
  org, orgPublic   (거래선·회사명과 그 공개 표기)

Leg (구간) — Trip 안의 도시 단위 방문
  tripId, seq, placeId, arriveDate?, departDate?, transport?, note

Place (장소 마스터)
  id, nameKo, nameLocal, aliases[], country, region
  lat, lng, googlePlaceId, type: city | site | restaurant | hotel
  (파생) firstVisitDate, visitCount

Story (이야기) — 여정에 붙는 글
  tripId?, legId?, source, url, title, date, body

Photo
  tripId?, legId?, takenAt(EXIF), lat?, lng?(EXIF GPS)
  caption, thumbDataUri, originalPath, autoMatched(bool)

ImportCandidate — 수집했지만 아직 승인 전 (서재와 동일 패턴)
```

**서재와 결정적으로 다른 점:** 서재의 책은 서로 독립이지만, 여기서는 Trip → Leg → Place가 **다대다로 얽힌다.** 도시 하나에 20년치 방문이 매달린다(베이징은 주재·출장·가족여행이 모두 걸린다). 그래서 "베이징"이 하나의 독립된 화면이 되어야 한다.

## 7. 사진 — EXIF 자동 배속

이 앱의 킬러 기능이다. 사진첩 폴더를 통째로 끌어놓으면:

1. **EXIF 촬영일시**를 읽어 날짜가 겹치는 Trip을 찾는다 → 자동 배속
2. **EXIF GPS**가 있으면 역지오코딩해서 Leg(도시)까지 확정한다
3. GPS가 없으면(대부분의 옛 사진) 날짜로 Trip만 맞추고 도시는 사용자가 고른다
4. 날짜도 안 맞으면 "어느 여정인지 모르는 사진" 보관함에 넣는다 — **여기서 오히려 잊고 있던 여정이 발견된다.** 사진이 있는데 여정이 없으면 "이때 어디 갔었나?"를 묻는다
5. 서재와 동일하게 **자동 확정 금지.** 배속 결과는 검토 화면을 거친다

이미지 처리는 서재 `scripts/prepare_photos.py`를 재사용한다(긴 변 1600px, EXIF 회전 보정 필수 — 원본 4000px·2.5MB는 그대로 못 쓴다). 다만 지도 마커용은 훨씬 작아도 되므로 **200px 썸네일을 따로 만든다.**

Drive의 20~30MB짜리 여행 일지 Docs에서 본문 이미지를 뽑는 경로도 같은 파이프라인에 태운다.

## 8. 지도 — Google Maps를 언제 쓰고 언제 안 쓰는가

서재에서 겪은 제약이 그대로 반복된다: **아티팩트 CSP가 외부 이미지·스크립트를 막는다.** 서재는 표지를 data URI로 바꿔 해결했다. 지도는 더 까다롭다. Google Maps JS API는 외부 스크립트 로드가 필수라 아티팩트 안에서 뜨지 않는다.

**그래서 Google Maps를 두 단계로 나눠 쓴다.**

| 시점 | 용도 | 호출량 |
|---|---|---|
| **빌드 타임 (1회)** | Geocoding API로 도시→좌표 확정, Places API로 Place ID·공식 명칭 확보 | 도시 수만큼. 수백 건 → 무료 한도 내 |
| **런타임 (앱 안)** | 호출하지 않는다. `data/places.json`의 좌표만 읽는다 | 0 |

앱의 지도는 **자체 SVG/Canvas 세계지도**로 그린다(세계 지도 + 중국 확대 레이어). 외부 타일을 안 받으므로 아티팩트에서도 뜨고, 오프라인에서도 뜨고, 30년치 점 300개를 한 화면에 찍어도 가볍다. 서재가 3D 서고를 직접 그린 것과 같은 선택이다.

**Next.js/Vercel 배포판**에서는 CSP 제약이 없으므로 여기에 진짜 Google Maps를 얹는다 — 실제 지도 타일, 스트리트뷰, 장소 사진, 경로선.

> 주의: 중국 내 Google Maps 좌표는 GCJ-02 오프셋이 있다. 도시 단위 점 찍기에는 문제없지만, 식당·호텔 단위로 내려가면 수백 미터가 어긋난다. 그 단계에서 보정이 필요하다.

## 9. 화면 설계

**첫 화면은 홈이다.** 지도·연표·지역은 그 뒤에 탭으로 붙는다.
아카이브가 쌓이고 나니 첫 화면이 조작판일 이유가 없어졌다 — 처음 온 사람에게 먼저 보여 줄 것은
필터와 슬라이더가 아니라 **어디를 다녔고 무엇을 안내할 수 있는지**다(§14).
조작판은 "지도" 탭 한 번 거리에 그대로 있다. 왼쪽 메뉴(시대·성격·목적)는 조작판 화면에서만 나온다.

0. **홈 (기본)** — 지구본 표지(§19) + 한 줄 요약 + 숫자 띠(도시·나라·여정·해·시대) →
   **다시 가고 싶은 곳**(3D 투어 12곳, 격자로 바꾸면 대표 도시가 큰 카드로 · §18) →
   **최근의 여정** → **안내할 수 있는 길**(모집 코스) → **동행 문의** → 출처.
   문구·대표 도시·코스는 `data/site.json` 한 파일에서 고친다. 데이터는 건드리지 않는다.
   표지 아래 찢긴 종이와 숫자 띠의 그림은 원화 한 장에서 오려 낸 조각이고(`scripts/crop_design.py`),
   **원화에 그려진 글자는 쓰지 않는다** — 제목·소개·버튼·숫자는 화면에서 글자로 다시 쓴다
1. **여행지 뷰** — 다녀온 도시를 사진 카드로. 나라별로 걸러 본다. 사진과 소개는 trip.com(§14)
2. **지도 뷰** — 세계지도에 도시 점. 점 크기 = 방문 횟수, 색 = 목적(출장/주재/가족/부부/친구). 상단 연도 슬라이더를 끌면 **점이 시간순으로 켜진다.** 중국을 누르면 확대 레이어로 전환(중국만 100여 개 점이라 세계 축척에서는 뭉갠다)
3. **연표 뷰** — 세로 연도축 1996~2026, 가로 7개 시대 레인. 경력 띠와 여정 점이 같은 화면에 겹친다. "홈IoT 하던 해에 아홉 번 나갔다"가 눈으로 보인다
4. **지역 뷰** — 대륙 > 국가 > 도시 트리. 국내/해외 탭 분리
5. **도시 페이지** — 도시 하나의 모든 방문 이력, 사진, 글. 머리에 그 도시의 trip.com 사진과 소개가 붙는다. 베이징처럼 20년이 쌓인 곳이 이 앱의 하이라이트
6. **여정 상세** — 일정표, 동행, 구간 지도(미니맵), 사진 그리드, 블로그 원문 링크. 넓은 화면은 오른쪽 패널, 휴대폰은 아래 시트(서재와 동일)
7. **통계** — 방문 국가 수, 도시 수, 총 이동 거리, 첫 방문 도시, 최다 방문 도시, 연도별 출국 횟수

휴대폰 대응은 서재의 규칙을 그대로 가져온다(☰ 서랍 메뉴, 아래 시트, 입력칸 16px로 iOS 확대 방지, 안전 영역, 화소 비율 1.5).

## 10. 로드맵

> 진행 상황(2026-09-20): Phase 1 완료 · **Phase 2 완료** · Phase 3 완료 · Phase 4 일부(사진 EXIF 배속).
> 배포: https://holyjoekang.github.io/my-travel-map/ (공개 빌드) · 저장소 holyjoekang/my-travel-map
> 빌드는 `python scripts/build_all.py`, 검증은 `python -m unittest discover -s tests`. 자세한 건 README.

서재의 교훈을 그대로 적용한다 — **가장 빨리 채워지는 소스를 1순위로.** 여기서는 Drive 출장표 28건이 이미 표로 정리되어 있어 LLM 없이 바로 들어간다.

1. **Phase 1 — 즉시 채우기 (LLM 불필요)**
   (a) **완료 ✅** — 지도 저장 목록 128개를 `data/gmaps_saved_2005.json`에 수집했다. 도시 63 · 장소 60 · 확인필요 12. 남은 일은 빌드 타임 지오코딩으로 좌표를 붙이는 것.
   (b) Drive 출장 스프레드시트 28건 + 최근 여행 Docs 약 15건 파싱 → `data/trips.json` 시드.
   이 단계만으로 지도에 점 100개 이상이 찍힌다.
2. **Phase 2 — 블로그 수집 (LLM 판정) ✅**
   687편 → 규칙으로 575편 자동 판정, 나머지 109편만 LLM. **여행 후보 112건** 추출.
   앱의 '후보' 탭에서 승인해야 여정이 된다(자동 등록 금지).
   cat 21·17·1 687편 수집 → "이 글이 특정 여정의 기록인가" 판정 → 여정/도시/날짜 추출 → **검토 화면에서 승인.** 오탐이 반드시 생긴다(cat 17의 시사 칼럼, cat 21의 러닝·영화 글).
   같은 판정에서 **인명·거래선 후보도 함께 뽑아** `Person`을 채운다(§11).
3. **Phase 3 — 지도·연표·지역 뷰 ✅**
   자체 SVG 지도 + 연도 슬라이더 + 시대 레인 연표. E3 도시들은 날짜 없이 묶음으로 표시.
4. **Phase 4 — 사진 배속 (일부 ✅)**
   EXIF 파이프라인, 끌어놓기 업로드, "여정 모르는 사진" 보관함.
5. **Phase 5 — 공백 구간 복원**
   `data/memory_trips.json`. **E1~E2(1996~2004) 전용 입력 화면** — 첫 싱가포르·말레이시아 출장, 런던, 뉴욕/뉴저지.
   E3 도시 중 날짜가 붙지 않은 것들에 기억으로 시기를 달아주는 작업도 여기서 한다.
   여권 스탬프 사진에서 입출국일을 읽는 보조 경로도 검토한다.
6. **Phase 6 — 공개 빌드 · 통계 · 이야기 연결**
   `Person.publicLabel` 지정 화면, `summaryPublic` 작성, `--public` 빌드와 실명 검사(§11).
   중국 원고와 여정 연결, 공유.

## 11. 공개 모드와 실명 마스킹

출장 기록에는 거래선·동료의 실명과 업무 내용이 그대로 들어 있다. Drive 출장표의 실제 문장이 이렇다.

```
○○ △사장 사업미팅
○○○과 출장, 첫 인도 출장, ○○상무 현지 미팅, 환대
○○○상무와 마지막 출장, 거래선 CEO 미팅, 방콕 물류거점 방문
```

**공개할 때는 실명을 쓰지 않고 `Mr.` / `Ms.`로 표기한다.** 설계상 중요한 점 세 가지.

**(1) 마스킹은 화면이 아니라 빌드에서 한다.** 이 앱은 데이터를 HTML에 넣어 배포한다(서재와 동일). 실명을 JSON에 담아두고 화면에서만 가리면 **소스 보기로 그대로 읽힌다.** 그래서 `scripts/build_app.py`에 빌드 모드를 둔다.

| 빌드 | 내용 | 산출물 |
|---|---|---|
| `--private` (기본) | 실명 그대로 | 본인만 여는 파일 |
| `--public` | **`Person.realName`이 번들에 아예 들어가지 않는다.** `publicLabel`만 주입 | 공유용 |

공개 빌드는 `realName`이 하나라도 남아 있으면 **빌드를 실패시킨다.** 사람이 눈으로 확인하는 방식에 기대지 않는다.

**(2) Mr./Ms.는 자동으로 붙이지 않는다.** 이름만으로 성별을 판단하면 틀린다. 그래서 수집 단계에서는 실명을 `Person`으로 뽑아두기만 하고, **`publicLabel`은 사용자가 사람 관리 화면에서 직접 정한다.** 정하지 않은 사람은 공개 빌드에서 관계에 따른 중립 표기로 나간다 — 동료 A, 거래선 B, 가족. 성별을 모른 채로도 공개 빌드가 만들어져야 하기 때문이다.

**(3) 지워야 할 것이 이름만은 아니다.** 마스킹 대상은 네 가지다.

- **인명** → `Mr. K` / `Ms. J`, 또는 중립 표기
- **직책이 붙은 호칭** — "△사장", "○○상무"처럼 성+직책이 한 덩어리로 붙어 있다. 정규식으로 못 잡는다. **수집할 때 LLM이 인명 후보를 표시**하고 사용자가 승인한다
- **거래선·회사명** — 상호 그 자체. `Person.orgPublic`으로 치환하거나 "현지 파트너"로 일반화
- **업무 내용** — "물류거점 방문", 제품·사업 내용. 여기는 치환이 아니라 **다시 쓰는 게 맞다.** 그래서 `Trip.summaryPublic`을 따로 둔다. 공개 빌드는 `summary`를 무시하고 `summaryPublic`만 내보낸다. 비어 있으면 그 여정은 제목·도시·날짜만 나간다

블로그에서 온 이야기(`Story`)는 이미 본인이 공개한 글이므로 원문 링크만 내보내고 본문은 공개 빌드에 싣지 않는다.

> 재직 중이므로, 사업 내용이 담긴 여정을 외부에 공개하기 전에 회사 기준을 확인하는 것이 안전하다. 마스킹 기능이 그 판단을 대신하지는 않는다.

## 12. 기술 스택

서재와 같은 스택을 쓴다. 이미 검증되었고 스크립트를 재사용할 수 있다.

- **수집**: Python (`scripts/`). `fetch_blog_posts.py`·`prepare_photos.py`는 거의 그대로 재사용
- **판정/추출**: Claude API (블로그 글 → 여정 판정 및 날짜·도시 추출)
- **좌표**: Google Geocoding + Places API — **빌드 타임에만**. `GOOGLE_MAPS_API_KEY` 사용자 환경변수
- **앱(1차)**: 단일 HTML 아티팩트 + `scripts/build_app.py`가 데이터를 data URI로 주입. 저장은 아티팩트 `db`(문서 `travel/user`), 실패 시 브라우저 저장으로 대체 — 서재와 동일
- **앱(2차)**: Next.js + Vercel. 여기서 진짜 Google Maps
- **EXIF**: Pillow + piexif

## 13. 남은 확인 사항

1. **저장 목록의 `needsReview` 12건.** 상위 도시가 불분명한 장소들이다(황궈수 폭포, 주자이거우, 창바이산 공항, Dongshan Hotel, Chunguang Shopping Plaza 등). 지도에서 확인하거나 기억으로 채워야 한다.
2. **Place ID가 필요한가.** 지금은 이름+행정구역만 있고 좌표는 빌드 때 지오코딩으로 붙인다. 도시 단위로는 충분하지만, 식당·호텔까지 정확히 찍으려면 Takeout CSV(URL 열에 CID)가 있어야 한다.
3. **저장 목록에 없는 도시가 더 있나.** 63개는 기억하시는 "100여 개"보다 적다. 당시 갔지만 지도에 저장하지 않은 도시가 있다면 §10 Phase 5에서 기억으로 보탠다.
4. **'힐링 모먼트' 1,397편에 어떻게 접근하나?** 블로그 최대 덩어리인데 목록 API가 0건을 돌려준다. 사진이 많다면 이 앱의 주요 소스다.
5. **`shinjoe.kang@gmail.com` 캘린더는 언제부터인가?** 2015년 이전까지 거슬러 올라간다면 날짜 정확도가 가장 높은 소스가 된다.
6. **1996~2004 첫 출장들의 연·월을 어디까지 특정할 수 있나?** 싱가포르·말레이시아 첫 출장, 런던, 뉴욕/뉴저지 — 연도만이라도 확정되면 연표가 완성된다. 이제 진짜 공백은 여기뿐이다.
7. **공개 범위 — 일부 결정됨.** 저장소를 공개로 두되 **실명과 업무 요약은 `data/private.json`(gitignore)로 분리**했다.
   Pages에는 공개 빌드만 올라간다. 남은 결정은 출장 여정의 **날짜·도시 자체**를 계속 공개할지다.
8. **배포 형태 — 1차 완료.** 단일 HTML + GitHub Pages로 떴다. 진짜 Google Maps가 필요하면 Next.js 2차로 간다.

## 14. 여행지 사진과 소개 — trip.com

30년치 기록은 도시 이름과 날짜다. 처음 보는 사람에게 "샤먼 시 · 5회"는 아무 그림도 그려 주지 않는다.
그래서 **다녀온 도시의 얼굴을 trip.com 여행 가이드에서 빌려 온다.**

**규칙 셋.**

1. **화면이 아니라 빌드가 받는다.** `scripts/fetch_destinations.py` 가 목적지 페이지에서
   표지 사진 주소·소개 글·명소 사진 주소를 뽑아 `data/destinations.json` 에 캐시한다.
   앱은 이 캐시만 읽는다. 화면이 뜰 때 trip.com 을 부르지 않는다
2. **사진은 복사하지 않고 링크로 건다.** 번들에는 주소만 들어간다.
   소개 글도 전문이 아니라 260자 발췌만 싣고, 카드마다 `Trip.com 가이드 →` 로 원문을 건다.
   출처는 카드·도시 패널·바닥글 세 곳에 밝힌다
3. **없어도 돈다.** 사진을 못 받으면(오프라인·차단·CSP) 그 자리는 흙빛 바탕에 도시 이름만 남는다.
   지도·연표·통계는 애초에 밖을 타지 않으므로 그대로다
4. **목록의 기준은 사진이 아니라 다녀온 도시다.** 사진 캐시로 세면 가이드가 없는 도시가
   통째로 빠져 나라 수·도시 수가 통계 탭과 어긋난다 — 미국 8곳이 뉴욕 하나로 세지던 것이
   그 예다. 여행지 목록은 도시 원장에서 오고, 사진은 있으면 얹는다

**§8과의 관계.** "외부를 아예 안 받는다"는 원칙이 여기서 한 칸 물러선다 —
**스크립트와 스타일은 여전히 파일 안에 있고, 밖에서 받는 것은 `ak-d.tripcdn.com` 의 사진뿐이다.**
테스트가 이 한 곳만 허용하고 나머지 주소를 잡는다(`test_images_only_from_tripcom`).
아티팩트처럼 외부 이미지를 막는 자리에서는 사진만 빠지고 앱은 그대로 뜬다.

**도시를 더 넣으려면** trip.com 에서 그 도시 가이드를 열고 주소 끝 숫자를
`data/tripcom_places.json` 에 적은 뒤 `python scripts/fetch_destinations.py` 를 돌린다
(`.../destination/beijing-1/` 의 `1`). trip.com 에 가이드가 없는 곳(남해·부여·평택·유명산 등)은
비워 둔다 — 사진 없이도 화면은 그대로 돈다.

**모집을 염두에 둔 설계.** 홈의 **안내할 수 있는 길**과 **동행 문의**는
`data/site.json` 의 `offers` · `invite` 에서 온다. 연락처는 적은 것만 버튼으로 나가고,
비어 있으면 버튼 대신 안내 문구가 나간다. **공개 빌드에도 그대로 나가므로 공개해도 되는 것만 적는다.**

## 15. 일정표 — 여정을 누르면 그날의 계획이 열린다

여정 원장은 "언제 어디에 갔는가"만 적는다. 그런데 여행마다 따로 만들어 둔
**일정표 한 장**(HTML)이 있다 — 비행편, 하루하루 동선, 지도, 먹을 것까지 들어 있는 문서다.
이것을 아카이브 밖에 두면 둘은 영영 만나지 않는다. **그래서 여정에 붙인다.**

**규칙 셋.**

1. **붙이는 일은 규칙이 한다 — LLM을 쓰지 않는다.** `scripts/build_itineraries.py` 가
   `<title>`·`<h1>`·본문에서 제목·기간·도시를 뽑고(도시는 장소 마스터의 이름과 별칭으로 맞춘다),
   **도시가 겹치고 날짜가 30일 안**인 여정을 고른다. 빌드 한 번이 1초 안에 끝난다
2. **짝이 없으면 여정을 새로 적는다.** 아직 다녀오지 않은 여행도 원장에 `planned: true` 로
   들어가 연표·홈에 `예정` 으로 선다. 여행이 끝나면 그 여정에 사진과 요약을 덧붙이면 된다.
   손으로 정한 짝(`trips[].itinerary`)이 규칙보다 앞선다
3. **개인 일정표는 공개본에 넣지 않는다(§11).** 공개 폴더는 `data/trip_html/`,
   개인 폴더는 `data/trip_html/private/`(gitignore)다. 공개 폴더에 이름+직책·전화·이메일·
   집 호수 형태가 보이면 빌드를 멈춘다. 테스트가 공개 번들에 개인 일정표가 섞였는지 다시 본다

**화면.** 일정표가 붙은 여정은 카드와 연표 배지에 `일정표` 표가 뜬다.
여정을 누르면 상세 패널 맨 위에 일정표 카드가 서고, 누르면 그 문서가 새 탭에서 열린다.
일정표 쪽에는 `← 나의 여행 지도` 한 줄을 넣어 돌아오게 한다.
일정표 문서 자체는 손대지 않는다 — 원본 그대로 `app/itinerary/<여정id>.html` 로 옮긴다.

## 16. 일정표 에이전트 — 내용을 주면 문서가 된다

§15는 "일정표가 있으면 여정에 붙인다"는 규칙이다. 그런데 **일정표 자체를 매번 손으로 짜는 것**이
남는다. 30년치 기록의 원문은 이미 블로그와 구글 문서에 있다. 그것을 옮겨 적는 일을 없앤다.

`scripts/itinerary_agent.py` 가 여섯 걸음으로 돈다.
**수집 → 구조화 → 검증 → 렌더 → 연동 → 배포.** 걸음마다 무엇을 했는지 한 줄씩 찍는다.

**규칙 넷.**

1. **원고 스펙이 가운데 있다.** 에이전트는 무엇을 읽든 먼저 사람이 읽을 수 있는 원고
   (`제목:`·`기간:`·`## DAY n`·`시각 | 무엇 | 메모`)로 옮기고, 그 원고에서 HTML을 만든다.
   원고는 `data/trip_source/` 에 남으므로 **고쳐서 다시 돌릴 수 있다.** 결과가 마음에 안 들면
   원고 한 줄을 고치지, 에이전트를 고치지 않는다
2. **HTML은 언제나 파이썬이 만든다.** LLM은 줄글을 하루 단위로 나눌 때(`--api`)만 부른다.
   생김새가 매번 달라지지 않고, 판정이 필요 없는 대부분의 일은 공짜로 1초 안에 끝난다.
   모델에게는 "없는 사실을 지어내지 마라"고 못박는다 — 원문에 있는 것만 옮긴다
3. **사실관계는 원장이 이긴다.** `--trip` 으로 기존 여정에 붙이면 기간·도시·제목을 그 여정에서
   가져오고(블로그를 올린 날이 아니라 여행한 날로 DAY 날짜를 셈한다), 원장에 짝을 못 박는다.
   표지 사진은 그 도시의 trip.com 사진을 쓰고 출처를 밝힌다(§14)
4. **개인정보는 묻지 않고 뺀다(§11).** 렌더한 문서에서 이름+직책·전화·이메일·집 호수 형태가
   보이면 공개 폴더가 아니라 `data/trip_html/private/` 로 보내고 그렇게 했다고 알린다.
   공개 여부는 사람이 `공개: 아니오` 로도 정할 수 있다

**입구 셋.** 손으로 쓴 원고(`.txt`), 네이버 블로그(`--blog logNo …`, 캐시된 본문을 쓰고
`--full` 이면 전문을 다시 받는다), 구글 문서(`--gdoc` — 웹에 게시된 문서 주소이거나
내려받은 `.txt`/`.html`). 표준입력으로 붙여 넣어도 된다.

**확인.** 첫 실전은 충칭 여행기 5편(0~4일차)이었다. 제목의 `n일차`를 읽어 DAY 1~5로 나누고,
여행한 날짜(6/3~6/8)를 붙이고, 원문 링크 다섯 개를 바닥에 달아 `t-2026-06-03` 에 붙었다.

## 17. 구글 지도 — 원칙을 지키면서 곁들이기

§8은 "지도는 밖을 안 탄다"이다. 국경 폴리곤을 번들에 넣고 SVG로 직접 그려서
오프라인에서도 뜨고 CSP에도 걸리지 않는다. 그런데 **확대해서 들여다보고 싶다**는
요구는 그 방식으로는 풀리지 않는다. 그래서 §14(trip.com 사진)와 같은 방식으로 한 칸 물러선다.

**규칙 넷.**

1. **기본은 여전히 번들 지도다.** 첫 화면도, 키가 없을 때도 SVG 지도로 그린다.
   구글 지도는 `구글 지도` 단추를 눌렀을 때만, 그때 처음으로 스크립트를 받아 온다
2. **못 받아 오면 되돌아간다.** 네트워크·키 제한·결제 문제로 실패하면 한 줄 알리고
   번들 지도로 돌아간다. 화면이 비지 않는다
3. **키는 저장소에 넣지 않는다(§11의 연장).** 개인 빌드는 `data/maps.json`(gitignore)에서
   읽고, 공개 빌드는 그 파일을 보지 않는다. 공개본에 넣으려면 CI 환경변수
   `GOOGLE_MAPS_KEY` 로만 들어가고, 그 키에는 HTTP 리퍼러 제한이 걸려 있어야 한다
4. **표시 규칙은 같다.** 점 크기는 방문 횟수, 색은 목적 — 두 지도가 같은 문법으로 말한다.
   점을 누르면 같은 상세 패널이 열린다

테스트가 이 선을 지킨다 — 번들에 `<script src=`·`<link rel="stylesheet"` 가 없어야 하고,
`maps.googleapis.com` 은 게으른 로더 한 곳에만 있어야 하며, 공개 빌드의 `mapsKey` 는 비어 있어야 한다.

## 18. 3D 여행지 투어 — 홈의 '다시 가고 싶은 곳'

카드를 격자에 눕혀 두는 대신 **둥글게 세워 돌린다**(three.js). 여행지는 사진이 주인공이라
한 장을 크게 보여 주고 그 양옆에 다음 도시를 걸쳐 두면, 목록이 아니라 **둘러보는 일**이 된다.

**규칙은 §17과 같다 — 곁들이다.**

1. **three.js 는 홈에 들어올 때 코드로 한 번만 받는다.** `<script src=` 태그는 없다.
   번들은 여전히 파일 하나로 돌고, 나머지 화면은 three.js 를 전혀 모른다
2. **못 받으면 격자가 그대로 화면을 맡는다.** 스크립트 실패·WebGL 없음·컨텍스트 손실 셋 다
   한 줄 알리고 접는다. 격자는 처음부터 같은 자리에 들어 있다(투어가 뜨면 CSS로 접을 뿐이다)
3. **문법은 격자와 같다.** 같은 trip.com 사진·소개, 같은 방문 횟수 딱지, 카드를 누르면
   같은 상세 패널. 사진·소개의 출처(© Trip.com)는 투어 설명줄에 그대로 남는다(§14)
4. **손도 키도 받는다.** 끌기·화살표 단추·좌우 화살표 키·Enter. 가만히 두면 천천히 돌고,
   손을 얹으면 멈춘다. `prefers-reduced-motion` 이면 저절로 돌지 않는다.
   보이지 않을 때(다른 탭·화면 밖)는 그리지 않고 쉰다
5. **되돌아갈 단추가 있다.** 섹션 머리의 `격자로 보기`/`3D 투어로 보기` — 보던 자리는 그대로 둔다

테스트가 이 선을 지킨다 — three.js 로더는 한 곳뿐이어야 하고, 홈의 그 섹션에는
투어 자리(`id="tourStage"`)와 격자(`class="dgrid"`)가 **함께** 들어 있어야 한다.

## 19. 지구본 표지 — 홈의 첫 화면

첫 화면이 보여 줄 것은 **어디를 다녔는가**다. 그래서 표지를 지구본으로 세운다.
다녀온 도시(도시급 99곳 — 장소·호텔·공항은 빼고)를 점으로 찍고, 점 크기는 다녀온 횟수다.
글(제목·소개·단추)은 왼쪽, 지구본은 오른쪽 아래로 크게 걸쳐 잘린다. 휴대폰은 지구본이 위, 글이 아래.

**규칙은 §18과 같다 — 곁들이다.**

1. **정지 그림이 먼저 선다.** 번들의 국경선(`world`)으로 정사영 지구본 SVG를 그려 표지 자리에 둔다.
   three.js 를 받으면 같은 방향·같은 크기의 돌아가는 지구본이 그 위에 겹쳐 뜨고 정지 그림은 물러난다.
   못 받거나 WebGL 이 없거나 컨텍스트를 잃으면 정지 그림이 그대로 표지를 맡는다. 점을 누르는 것도 된다
2. **three.js 는 투어와 같은 로더로 한 번만 받는다.** 지구본 라이브러리(globe.gl·three-globe)는 들이지 않는다
   — three 를 한 벌 더 끌고 온다. 텍스처 사진도 받지 않는다. 땅은 번들 국경선을 캔버스에 칠해 입히고,
   색은 테마 토큰(`--globe-*`)을 따른다
3. **점은 처음 간 해의 순서대로 켜진다.** 들어올 때 한 번 1996 → 2026 으로 켜지고(약 2.4초),
   숫자 띠의 도시 수가 같이 오른다. 연도 막대를 끌면 그해의 지구본이 된다 — 지도 탭의 연도 슬라이더와 같은 뜻
4. **문법은 지도 탭과 같다.** 점에 올리면 이름·횟수·처음 간 해, 누르면 같은 도시 패널.
   서울은 출발점 고리, 다음 예정 여정은 서울에서 이은 점선 호와 표지 아래 쪽지로 선다
5. **손을 뺏지 않는다.** 마우스로는 끌어 돌리고 화살표 키도 받는다. 휴대폰은 끌어 돌리지 않고 탭만 받는다
   — 세로 스크롤은 페이지의 것이다. 가만히 두면 천천히 돌고(한 바퀴 4분), 올리면 멈춘다.
   `prefers-reduced-motion` 이면 켜지는 연출도 자전도 없다. 화면 밖·다른 탭이면 그리지 않는다

테스트가 이 선을 지킨다 — 표지에 정지 지구본(`globeSVG()`)과 3D 자리(`id="globeStage"`)가 함께 있어야 하고,
지구본은 `loadThree()` 로만 three.js 를 받으며, 번들이 globe.gl·three-globe·unpkg·텍스처 사진을 부르지 않아야 한다.
